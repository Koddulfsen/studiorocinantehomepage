"use client";

// GBP Score Calculator — built step by step.
// Step 0: scaffold only. Steps 1–11 add the real UI.

export function GbpCalculator() {
  return (
    <div className="rounded-xl border border-ink/10 bg-white/60 px-8 py-10 text-center">
      <p className="font-mono text-xs uppercase tracking-widest text-ink-soft">Coming soon</p>
      <p className="mt-3 font-serif text-2xl font-semibold">How visible are you on Google?</p>
      <p className="mt-3 text-sm leading-relaxed text-ink-soft">
        Search your business and get an instant score based on 9 real ranking factors.
      </p>
    </div>
  );
}
